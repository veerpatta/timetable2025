# One-Off Helpers

This folder contains historical helper scripts and an old patch artifact that were used during earlier maintenance passes.

They are kept for traceability, but they are not part of:

- the runtime application
- the normal development workflow
- the test suite

Most tasks should ignore this folder. If you add a temporary migration or fix script in the future, put it here instead of the repo root.
